import { combineReducers } from 'redux';

import { profileReducer as profile } from './profileReducer';
// reducers from future api endpoints to be integrated

export default combineReducers({
  profile
});
